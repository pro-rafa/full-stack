//bloco if

// if pode ser utilizado sozinho
// sempre que utilizo a palavra else, preciso de um if antes
// Eu posso ter varios else ifs na checagem
// só posso ter um else na checagem
// podemos usar condiçoes sem o else if, utilizando apenas o if e else


/*const hora = 1;

if (hora <= 12) {
    console.log('bom dia')
} else if (hora >= 12 && hora <= 17) {
    console.log('boa tarde')
} else if (hora >= 18 && hora <= 23) {
    console.log('boa noite')
} else {
    console.log('olá')
}*/


/*const tenhoGrana = 1

if (tenhoGrana) {
    console.log('vou sair de casa')
}*/

/*let idade = 18;
let voto = idade >= 18 ? "Pode votar" : "Não pode votar"; //operador ternario
console.log(voto); // Saída: "Pode votar"*/


let nota = 50
let resultado = nota >= 90 ? "Excelente" : nota >= 80 ? "Ótimo" : nota >= 70 ? "Bom" : "Insuficiente";

console.log(resultado);

